# Google Drive credentials for the weekly backup job (hitch/scripts/backup_to_drive.py).
#
# Copy to deploy/rclone.conf and fill in a real token. That file is gitignored and mounted
# into the container at /root/.config/rclone/rclone.conf — it must NOT be committed and must
# NOT be baked into the image.
#
# The mount is deliberately read-write: rclone refreshes the OAuth access token and writes
# the new one back to this file. Mounting it :ro makes uploads fail once the initial access
# token expires (~1h after you create it), which looks like a working setup that breaks a
# week later.
#
# Create this file BEFORE the container next starts. Docker bind-mounts a missing source path
# by creating it as an empty *directory*, so a `docker compose up` with no rclone.conf leaves
# a root-owned directory here and rclone silently runs with no remotes configured.
#
# To generate the token, on a machine with a browser (NOT this headless server):
#
#   rclone config
#     n) New remote
#     name> gdrive
#     Storage> drive
#     client_id>            (see the rate-limit note below)
#     client_secret>        (blank)
#     scope> 1              (full access — needed for the prune step to delete old backups)
#     root_folder_id>       (blank, or the ID of a specific folder from its Drive URL)
#     service_account_file> (blank)
#     Edit advanced config? n
#     Use auto config? y    -> opens a browser, log in as the account that owns the backups
#     Configure this as a Shared Drive? n
#
# Then `rclone config file` shows the path; copy the [gdrive] block here, and set in .env:
#
#   BACKUP_RCLONE_REMOTE=gdrive:hitchwiki_maps-backups
#
# Create that folder in Drive first (`rclone mkdir gdrive:hitchwiki_maps-backups`). Verify with:
#
#   sudo docker exec hitchhiking-map python3 /app/hitch/scripts/backup_to_drive.py --no-upload
#   sudo docker exec hitchhiking-map rclone lsd gdrive:
#
# Rate limits: a blank client_id means you share rclone's global default OAuth client with
# every other rclone user, whose per-minute quota is frequently exhausted — it surfaces as
# "Error 403: Quota exceeded ... consumer project_number:202264815644". Ours is currently
# blank and works, because one upload a week is a handful of API calls and the job retries.
# If the weekly run starts failing on 403s, create a personal OAuth client (Google Cloud
# Console -> APIs & Services -> enable Drive API -> OAuth client ID, type "Desktop app") and
# re-run `rclone config` with that client_id/client_secret. That gives this project its own
# quota and is the standard fix.

[gdrive]
type = drive
scope = drive
token = {"access_token":"...","token_type":"Bearer","refresh_token":"...","expiry":"..."}
team_drive =
