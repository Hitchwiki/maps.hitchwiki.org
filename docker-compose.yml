services:
  hitchhiking-map:
    build: .
    container_name: hitchhiking-map
    ports:
      - "5000:5000"
    volumes:
      - ./db:/app/db
      - ./logs:/app/logs
      - ./dist:/app/dist
    environment:
      # Add any environment variables here
      EXAMPLE_ENV: value
